/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ["images.unsplash.com"],
  //    // if using external images
  // },
   allowedDevOrigins: ['10.242.69.119'],
};

export default nextConfig;